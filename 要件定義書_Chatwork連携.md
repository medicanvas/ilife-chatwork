# 介護施設向け通院報告支援システム 要件定義書  
## Chatwork 連携

- **作成日**: 2026年3月17日
- **版**: 第1版
- **対象**: Carelife MVP における Chatwork 連携機能
- **前提**: 本ドキュメントは `20260307_要件定義書v02.md` を前提とし、LINE 連携に加えて Chatwork への報告送信・テキスト転送の要件を定義する。
- **実装状況**: 本要件は 2026年3月時点で実装済み。本番（Cloud Run）で稼働中。詳細は README・`docs/ハンドオーバー_20260307.md`・`docs/Chatwork連携テスト手順.md` を参照。

---

# 1. 文書の目的

本書は、介護施設向け通院報告支援システム（Carelife MVP）において、**Chatwork** を送信先・通知先として利用するために必要な機能要件・非機能要件を整理することを目的とします。  
既存の LINE 連携に加え、施設で利用されている Chatwork のルームへ報告を送信したり、LINE で受信したテキストを Chatwork に転送したりする振る舞いを定義します。

---

# 2. スコープ

## 2.1 対象とする機能

| 対象 | 内容 |
|------|------|
| **通院報告の Chatwork 送信** | 報告確認・編集画面（SC-11）で「送信する」を実行したとき、起動元に応じて LINE のみ／Chatwork のみ／LINE と Chatwork の両方へ送信する。 |
| **送信先の判別** | フロントエンドの URL クエリパラメータ（`userId`, `chatwork_room_id`）により、送信先を自動判別する。 |
| **LINE 受信テキストの Chatwork 転送** | LINE Bot が受け取ったテキストメッセージを、設定した Chatwork ルームへ転送する（任意設定）。 |

## 2.2 対象外（今後の拡張）

- Chatwork ルーム内への UI 埋め込み（API 仕様上不可）
- 患者ルームごとの報告の自動振り分け（要設計・別要件）
- Chatwork ログイン認証や OAuth 連携（本 MVP では API トークンのみ）

---

# 3. 機能要件

## 3.1 報告送信先の振る舞い

### 3.1.1 起動元による送信先

| 起動元 | URL の例 | 送信先 |
|--------|----------|--------|
| **LINE** | フロント URL に `userId=Uxxxxxxxx` が付与されている | **LINE と Chatwork の両方**。LINE は当該 userId へ Push、Chatwork は環境変数で指定したルーム（または既定ルーム）へ投稿。 |
| **Chatwork** | フロント URL に `chatwork_room_id=〇〇` が付与されている | **Chatwork の当該ルームのみ**。LINE には送信しない。 |
| **直接アクセス** | `userId` も `chatwork_room_id` もない | 送信先は **LINE も Chatwork も送信しない**、または運用方針に応じてエラー表示・案内とする。（実装では送信しない扱いとする。） |

### 3.1.2 要件一覧

- フロントエンドは、画面表示時に URL のクエリパラメータから `userId` および `chatwork_room_id` を取得し、送信時にバックエンドへ渡すこと。
- バックエンドは、受け取った `userId` の有無に応じて LINE Push メッセージの送信の要否を決定し、`chatwork_room_id` の有無（または指定）に応じて Chatwork への投稿の要否・投稿先ルームを決定すること。
- **LINE から開いた場合**（`userId` あり）: 報告文を LINE に送信するとともに、Chatwork の既定ルーム（環境変数 `CHATWORK_ROOM_ID`）にも同一内容を投稿すること。`chatwork_room_id` がリクエストで指定されていれば、そのルームにも投稿してよい（実装で対応する場合）。
- **Chatwork から開いた場合**（`chatwork_room_id` あり）: 報告文を当該 `chatwork_room_id` のルームにのみ投稿し、LINE には送信しないこと。
- 送信完了画面（SC-12）では、実際に送信した先（LINE／Chatwork／両方）が分かるメッセージを表示すること。

## 3.2 報告確認・編集画面（SC-11）および送信 UI

- 送信ボタンは **「送信する」** の 1 つとし、「LINEに送信する」「Chatworkに送信する」のように送信先ごとに分けないこと。送信先は URL パラメータから自動判別すること。
- 報告文の入力欄（テキストエリア）は、**文章量に応じて高さを自動調整**し、可能な範囲でスクロールを減らし、読みやすくすること。
- 各画面で **現在のステップ（例: ステップ 3 / 9）** を表示し、全体の進捗が分かるようにすること。高齢者・非 IT 利用者にも分かりやすい UX とすること。

## 3.3 LINE Bot による Chatwork 転送

- LINE Bot の Webhook で **テキストメッセージ** を受信したとき、設定（環境変数）で Chatwork 連携が有効な場合は、当該テキストを **指定した Chatwork ルームに転送**すること。
- 転送時は、送信者（LINE の userId）、本文、送信時刻などを Chatwork のメッセージ記法（例: `[info][title]...[/title]`）で整形して投稿すること。
- 転送先ルームは、環境変数 `CHATWORK_ROOM_ID` で指定する。未設定の場合は転送を行わないこと。
- 通院報告の **要約文（AI 要約後の報告文）** の Chatwork 送信は、フロントの「送信する」からバックエンドを経由して行うものであり、LINE Bot の「テキスト転送」とは別とする。すなわち、LINE で「報告」と送ったあとフロントで報告を作成し「送信する」を押したとき、要約文はバックエンドから LINE と Chatwork の両方に送信される。

---

# 4. 非機能要件・制約

## 4.1 Chatwork API

- **認証**: Chatwork API トークン（`x-chatworktoken` ヘッダー）を使用する。トークンは環境変数で管理し、リポジトリやクライアントに含めないこと。
- **エンドポイント**: メッセージ投稿には `POST https://api.chatwork.com/v2/rooms/{room_id}/messages` を使用する。
- **レート制限**: Chatwork API のレート制限に従うこと。連続投稿が必要な場合は間隔を考慮すること。

## 4.2 環境変数

| 変数名 | 設定先 | 説明 |
|--------|--------|------|
| `CHATWORK_API_TOKEN` | バックエンド・LINE Bot（Cloud Run の「変数とシークレット」等） | Chatwork API トークン。未設定の場合は Chatwork 投稿・転送を行わない。 |
| `CHATWORK_ROOM_ID` | 同上 | 既定の投稿先ルーム ID（数値）。LINE から開いた場合の Chatwork 送信先、および LINE Bot のテキスト転送先。 |

- 本番環境では GCP Cloud Run の「変数とシークレット」で設定すること。ローカルでは `backend/.env` および `line-bot/.env`（使用する場合）で設定する。`.env` は Git にコミットしないこと。

## 4.3 セキュリティ・運用

- API トークンは第三者に漏らさないこと。Cloud Run ではシークレット参照を推奨する。
- `chatwork_room_id` は URL に含まれるため、ルーム ID が推測されてもトークンが無ければ投稿できない設計であること。必要に応じてルームごとの権限は Chatwork 側で管理すること。

---

# 5. 画面・フローとの対応（v02 との整合）

- **SC-11 報告確認・編集画面**: 「LINEに送信する」に代えて **「送信する」** とし、送信先は URL パラメータで判別する。報告テキストエリアは内容に応じて高さを調整する。
- **SC-12 送信完了画面**: 送信先（LINE／Chatwork／両方）に応じた完了メッセージを表示する。
- **画面遷移**: v02 の「11. 画面要件」「12. 画面一覧」「14. 画面遷移の考え方」を継承し、上記 SC-11・SC-12 の変更のみを本要件で追加・更新する。

---

# 6. 受け入れ条件（Chatwork 連携）

- LINE の報告リンク（`?userId=...` 付き）から報告を作成し「送信する」を押したとき、報告文が **LINE と Chatwork の指定ルームの両方** に送信されること。
- Chatwork 用リンク（`?chatwork_room_id=〇〇` 付き）から報告を作成し「送信する」を押したとき、報告文が **当該 Chatwork ルームにのみ** 送信され、LINE には送信されないこと。
- LINE Bot で Chatwork 連携が有効なとき、LINE で送ったテキストが指定 Chatwork ルームに転送されること（任意設定）。
- 報告確認画面でステップ表示（例: ステップ 3/9）が表示され、報告テキスト欄が内容に応じて伸縮すること。
- 上記を満たす場合、Chatwork 連携の要件を満たしたものとする。

---

# 7. 今後の拡張（参考）

以下の機能は本版のスコープ外とするが、要件検討・設計の参考として記載する。

- **患者ルームへの振り分け**: 患者全体用ルームに投稿された報告を、内容（利用者名など）に基づき各患者用ルームへ自動投稿する。Chatwork Webhook でメッセージを検知し、バックエンドで患者名とルーム ID の対応を解決する構成が想定される。
- **テンプレート・AI 整形**: テンプレートをメッセージで配布し利用者にコピー入力を促す、または Chatwork への投稿を Webhook で受け取り AI で整形して返信する、といった運用は API 上可能。Chatwork クライアント内にフォームや UI を埋め込むことは API では不可。

---

以上、Chatwork 連携に係る要件定義書（第1版）とする。
