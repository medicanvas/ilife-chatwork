# 介護施設向け通院報告支援システム 要件定義書  
## Chatwork ルームからの報告書まとめ（デモ）

- **作成日**: 2026年3月19日
- **版**: 第1版
- **対象**: Carelife において、Chatwork の患者ルームに溜まった通院報告をまとめ、外部ヘルパー等に渡す「報告書」を生成・出力する**デモ機能**
- **前提**: 本ドキュメントは `20260307_要件定義書v02.md` および `要件定義書_Chatwork連携.md` を前提とする。既存の通院報告の Chatwork 送信は実装済み。
- **目的**: 試すための仕組みを整え、**出力形式まで**をデモとして実現する。本版では実装の詳細ではなく要件の整理に重きを置く。

---

# 1. 文書の目的

本書は、介護施設（Carelife）が **患者ごとの Chatwork ルームに蓄積した通院報告** を、**外部ヘルパー依頼時などに「これまでの通院報告をまとめた資料」として渡す** ための機能を、**デモとして試す** にあたり必要な要件を定義することを目的とします。

施設では患者ごとにルームを作成し、通院のたびに報告が投稿されていく運用を想定しています。毎回ではないが、患者を通院に連れて行く際に外部ヘルパーに依頼する場合、**過去の通院報告の内容をまとめたものを渡している** というニーズがあり、その「まとめ」がどのような形式・内容になるかを推定し、デモで出力まで試せるようにすることを目指します。

---

# 2. 業務背景・ユースケース

## 2.1 現状の運用イメージ

| 項目 | 内容 |
|------|------|
| **ルーム構成** | 施設は患者ごとに Chatwork ルームを作成し、その患者に紐づく通院報告をそのルームに投稿している。 |
| **報告の蓄積** | 通院のたびに Carelife の「報告作成 → 送信」フローで報告文が当該患者ルームに投稿され、時系列で溜まっていく。 |
| **外部ヘルパー依頼** | 患者を通院に連れて行く際、必ずしも毎回ではないが、外部ヘルパーに依頼することがある。 |
| **ニーズ** | その際に **「これまでの通院報告の内容をまとめたもの」** を外部ヘルパーに渡したい。 |

## 2.2 デモで実現したい流れ

1. **トリガー**: 施設担当者が「報告書を作りたい」と判断したとき、何らかの操作（例: 専用リンクのクリック、または画面からルーム指定）で処理を開始する。
2. **取得**: 指定した Chatwork ルーム（患者ルーム）の投稿一覧を API で取得する。
3. **成形**: 取得した投稿を「報告書」として読みやすい形に整形する（フィルタ・並び替え・見出し付けなど）。必要に応じて AI で要約・構成する。
4. **出力**: 成形結果を **プレビュー** で表示し、**編集** 可能にする。**印刷** および **保存**（またはエクスポート）ができるようにする。

※ Chatwork クライアント内にフォームや編集 UI を埋め込むことは API 仕様上不可なため、プレビュー・編集・印刷・保存は **Carelife の Web 画面（または別画面）** で行う想定です。

---

# 3. 「報告書」のイメージ（リサーチに基づく推定）

施設が外部ヘルパーに渡す「通院報告のまとめ」が実際にどの形式かは本ドキュメント作成時点では未確定のため、介護・訪問介護の一般的な **通院介助報告・申し送り・引き継ぎ** の慣行に基づき、以下のような内容・構成を**推定**として定義します。デモではこの形式をベースにし、実際の運用フィードバックで調整することを想定します。

## 3.1 報告書に含めると想定する項目

| 項目 | 説明 |
|------|------|
| **表題** | 「通院報告まとめ」「通院報告一覧」など。 |
| **利用者（患者）情報** | 氏名など。ルーム名や投稿内容から補完する場合あり。 |
| **対象期間** | まとめの対象とした期間（例: 直近3ヶ月、全期間）。 |
| **通院報告一覧（時系列）** | 各回の通院報告を **日付順**（新しい順または古い順）で列挙。 |
| **各回の内容** | 受診日、診察科・病院名（分かれば）、受診内容の要約（処方・検査・主治医の指示など）、特記事項。Carelife から投稿された報告文は要約形式であることが多いため、そのまままたは軽く整形して掲載。 |
| **注意事項・申し送り** | 必要に応じて、服薬・アレルギー・移動時の注意など、外部ヘルパーに知らせたい点をまとめた短文。 |

## 3.2 出力形式（デモの範囲）

- **プレビュー**: 上記を Web 画面上で **1 本の文章（テキストまたは簡易リッチテキスト）** として表示する。編集可能なテキストエリアまたはエディタで内容を修正できるようにする。
- **印刷**: ブラウザの印刷機能で印刷できる。印刷用にレイアウトを整えたビューを用意してもよい。
- **保存**: 編集後の内容を **テキストまたは PDF などでダウンロード** できるようにする。DB への保存はデモ範囲外としてもよい（任意で拡張可能とする）。

---

# 4. スコープ

## 4.1 本デモで対象とする機能

| 対象 | 内容 |
|------|------|
| **ルーム指定** | 報告書の元データとして、**1 つの Chatwork ルーム ID** を指定する。デモでは、URL クエリパラメータ（`chatwork_room_id`）または画面入力でルーム ID を渡す方式を想定。 |
| **メッセージ取得** | Chatwork API（`GET /rooms/{room_id}/messages`）で当該ルームのメッセージ一覧を取得する。 |
| **成形** | 取得したメッセージを日付順に並べ、報告書の体裁（表題・利用者・期間・一覧・各回の内容）に整える。通院報告らしき投稿の選別ルール（キーワードや形式）は簡易なものでよい。 |
| **プレビュー・編集** | 成形結果を Carelife の新規画面（例: 報告書プレビュー画面）で表示し、文言の編集を可能にする。 |
| **印刷** | ブラウザの印刷機能で印刷できる状態にする。 |
| **保存・エクスポート** | 編集後の内容をテキストファイルまたは PDF でダウンロードできるようにする。 |

## 4.2 本デモで対象外とするもの（将来拡張）

| 対象外 | 備考 |
|--------|------|
| **Chatwork 内でのトリガー** | 「報告書作成」などの文言投稿を Chatwork Webhook で検知して自動起動する方式は、デモでは必須としない。必要であれば別要件で追加。 |
| **複数ルームの一括** | 複数患者ルームを一度にまとめる機能は対象外。1 ルーム = 1 報告書を想定。 |
| **認証・権限** | デモでは、報告書プレビューへのアクセス制御は簡易にとどめ、必要に応じてトークン付き URL や既存認証の拡張で対応する。 |
| **報告書の永続保存（DB）** | 編集後の報告書をサーバー側 DB に保存する機能はデモ範囲外。ダウンロードによる保存を優先。 |

---

# 5. 機能要件

## 5.1 トリガー・入口（デモ）

- **方式 A（推奨）**: Carelife のフロントエンドに「報告書を作成」用の**専用リンクまたは画面**を用意する。リンクには `chatwork_room_id` をクエリで付与する（例: `.../report-summary?chatwork_room_id=123456`）。ユーザーは患者ルームにそのリンクを貼っておき、報告書を作りたいときにクリックして開く。
- **方式 B**: 既存の報告作成フローとは別に、「報告書まとめ」画面を設け、その画面で **ルーム ID を入力** して「取得してまとめる」を実行する。

いずれも、**指定された 1 つの `chatwork_room_id`** をバックエンドに渡し、以降の処理で使用する。

## 5.2 メッセージ取得

- **API**: `GET https://api.chatwork.com/v2/rooms/{room_id}/messages?force=1` を使用する（Chatwork API v2）。
- **仕様**: 1 回の呼び出しで **最大 100 件** まで取得可能。`force=1` で最新メッセージから最大 100 件を取得する。100 件を超える履歴がある場合は、ページネーション（`message_id` を基準にした追加取得）が API でサポートされているか確認し、デモでは「直近 100 件」のみを対象としてもよい。
- **認証**: 既存と同様、`CHATWORK_API_TOKEN` をヘッダー `X-ChatWorkToken` に付与する。
- **権限**: 当該ルームに参加しているアカウントのトークンである必要がある。

## 5.3 成形

- **入力**: 取得したメッセージの配列（各要素: `message_id`, `account`, `body`, `send_time`, `update_time`）。
- **処理**:
  - 送信日時（`send_time`）で **時系列に並べ替え**（新しい順または古い順は運用に合わせて選択）。
  - 通院報告として扱うメッセージの **選別**:
    - デモでは「すべてのメッセージを報告として扱う」でも可。または、本文に「通院」「報告」等のキーワードが含まれるもの、あるいは Carelife の Bot が投稿した形式のものに限定するルールを設けてもよい。
  - 表題・利用者名・対象期間・一覧見出し・各回の日付と本文を組み立て、**1 本のテキスト（報告書ドラフト）** を生成する。
- **AI の利用**: 任意。Gemini 等で要約や表現を整える場合は、既存の Carelife バックエンドのプロンプト・API を流用する想定。デモでは「ルールベースの整形のみ」でも可。

## 5.4 プレビュー・編集・印刷・保存

- **プレビュー**: 成形した報告書ドラフトを、Carelife の新規画面に表示する。テキストエリアまたはリッチテキスト風の編集可能領域で内容を表示し、ユーザーが文言を修正できるようにする。
- **印刷**: その画面をブラウザの印刷機能で印刷できるようにする。必要に応じて「印刷用レイアウト」の CSS を当てる。
- **保存**: 「ダウンロード」または「保存」ボタンを用意し、編集後の内容を **テキストファイル（.txt）** または **PDF** でダウンロードできるようにする。PDF はサーバー側で生成するか、クライアント側のライブラリで生成するかは実装で選択する。

## 5.5 エラー・空データ

- ルーム ID が無効、または API が 403/404 を返した場合: 画面にエラーメッセージを表示し、ルーム ID の確認を促す。
- メッセージが 0 件（204）の場合: 「該当ルームにメッセージがありません」等のメッセージを表示し、報告書は空の状態でプレビューを表示するか、操作案内を表示する。

---

# 6. 非機能要件・制約

## 6.1 Chatwork API

- **メッセージ一覧取得**: `GET /rooms/{room_id}/messages`、最大 100 件/回。`force=1` で最新 100 件を取得。
- **レート制限**: API 全体で 5 分あたり 300 回、メッセージ関連は 10 秒あたり 10 回などの制限があるため、連続呼び出し時は考慮する。
- **トークン**: 既存の `CHATWORK_API_TOKEN` を利用。環境変数で管理し、リポジトリに含めない。

## 6.2 セキュリティ

- 報告書プレビュー用 URL に `chatwork_room_id` を含める場合、URL の共有範囲に注意する。必要に応じて **有効期限付きトークン** を発行し、`room_id` をトークンに含めて隠す方式を検討する。
- API トークンはサーバー側のみで扱い、フロントには露出させない。

## 6.3 既存システムとの関係

- バックエンド: 既存の Carelife バックエンドに、**ルームメッセージ取得** と **報告書ドラフト成形** の API（例: `GET /api/carelife/report-summary?chatwork_room_id=xxx` や `POST` でルーム ID を渡す）を追加する形を想定。
- フロントエンド: 既存の Carelife フロントに、報告書プレビュー・編集・印刷・ダウンロード用の画面（またはルート）を追加する。

---

# 7. 画面・フロー（デモ）

## 7.1 想定フロー

1. ユーザーが「報告書を作成」リンク（`chatwork_room_id` 付き）をクリックする、または「報告書まとめ」画面でルーム ID を入力する。
2. フロントはバックエンドに「当該ルームの報告書ドラフトを生成してほしい」とリクエストする。
3. バックエンドは Chatwork API でメッセージ一覧を取得し、成形して報告書ドラフト（テキスト）を返す。
4. フロントは報告書プレビュー画面にその内容を表示し、編集可能にする。
5. ユーザーが編集し、「印刷」でブラウザ印刷、「保存」でテキストまたは PDF をダウンロードする。

## 7.2 画面イメージ（デモ）

| 画面 | 内容 |
|------|------|
| **報告書まとめ 入口** | リンクから遷移する場合は「報告書を生成しています」などのローディング表示ののち、プレビューへ。ルーム ID 入力式の場合は、入力欄と「取得してまとめる」ボタン。 |
| **報告書プレビュー** | 表題・利用者・期間・通院報告一覧（時系列）を表示。編集可能なテキスト領域。ボタン: 「印刷」「ダウンロード（.txt / .pdf）」など。 |

---

# 8. 受け入れ条件（デモ）

- 指定した Chatwork ルーム ID に対して、**メッセージ一覧取得 API が成功**し、取得した投稿を **時系列で並べた報告書ドラフト** が生成できること。
- 報告書ドラフトが **Carelife の画面でプレビュー** され、**編集** できること。
- **印刷** により、表示内容を紙として出力できること。
- **保存（ダウンロード）** により、編集後の内容をテキストまたは PDF で取得できること。
- ルーム ID が無効な場合やメッセージが 0 件の場合に、**適切なメッセージ** が表示されること。

---

# 9. 今後の拡張（参考）

- **Chatwork Webhook**: 「報告書作成」などの投稿を検知し、自動で報告書ドラフトを生成してプレビュー用リンクをルームに返す。
- **複数ルーム・患者一覧**: 施設で管理する患者ルーム一覧から選択して報告書を生成する。
- **報告書の DB 保存**: 編集済み報告書を施設・患者単位で保存し、履歴参照や再ダウンロードを可能にする。
- **テンプレート**: 施設ごと・用途ごとの報告書テンプレートを選べるようにする。

---

以上、報告書まとめデモに係る要件定義書（第1版）とする。
